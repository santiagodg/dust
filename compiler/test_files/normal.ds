static global_1: bool;
static global_2: f64;

fn main()
let local_1: i32;
let local_2: i32;
let local_char: char;
let local_arr: [f64; 2];
{
    global_2 = -3.17;
    global_1 = true;
    local_1 = 2;
    local_2 = 3;
    local_char = 'a';
    local_arr = [4.4; 2];

    if global_1 {
        local_1 = local_1 + local_2;

        while local_char == 'a' {
            local_2 = -4;
            local_char = 'b';
        };
    } else {
        local_1 = local_1 - local_2;
    };

    while local_1 < 20 {
        local_1 = local_1 + 1;
    };

    loop {
        local_2 = local_2 + 1;
        break;
    };

    if global_1 == false {
        local_char = 'b';
    };

    write(local_char);
}
